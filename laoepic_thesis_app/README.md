# Lao Epic (Mobile App)

Flutter client for the Lao Epic tourism booking platform.

## Package name

Dart imports use `package:laoepic_thesis_app/...` (see `pubspec.yaml`).

## Run locally

```bash
flutter pub get
flutter run
```

Copy `.env.example` to `.env` for API origin and Google Sign-In keys.
