import 'package:flutter/material.dart';

/// Lao Epic brand palette (use everywhere instead of hard-coded legacy blues/oranges).
abstract final class AppColors {
  static const Color primary = Color(0xFF084887);
  static const Color accent = Color(0xFFF58A07);
  /// Light surface tint for chips / action rows behind accent icons.
  static const Color accentSurface = Color(0xFFFFF4EC);
}
